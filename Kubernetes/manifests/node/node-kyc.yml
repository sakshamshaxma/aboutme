---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: production
  name: node-kyc-deployment
  labels:
    app: node-kyc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-kyc
  template:
    metadata:
      labels:
        app: node-kyc
    spec:
      containers:
       - name: node-kyc
         image: 919314377239.dkr.ecr.ap-south-1.amazonaws.com/spartankyc:37
         envFrom:
          - secretRef:
            name: kyc-secret
         volumeMounts:
          - name: tz-config
            mountPath: /etc/localtime
         resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
         limits:
           memory: "500Mi"
           cpu: "200m"
      imagePullSecrets:
      - name: ecr-secret
      volumes:
       - name: tz-config
         hostPath:
          path: "/usr/share/zoneinfo/Asia/Calcutta"
