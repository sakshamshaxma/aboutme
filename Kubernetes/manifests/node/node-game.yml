---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: production
  name: node-game-deployment
  labels:
    app: node-game
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-game
  template:
    metadata:
      labels:
        app: node-game
    spec:
      containers:
       - name: node-game
         image: 919314377239.dkr.ecr.ap-south-1.amazonaws.com/game:131
         envFrom:
          - secretRef:
                name: game-secret
         volumeMounts:
          - name: tz-config
            mountPath: /etc/localtime
         resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
         limits:
           memory: "500Mi"
           cpu: "200m"
      imagePullSecrets:
      - name: ecr-secret
      volumes:
       - name: tz-config
         hostPath:
          path: "/usr/share/zoneinfo/Asia/Calcutta"
